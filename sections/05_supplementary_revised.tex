\clearpage
\section*{Supplementary information}
\phantomsection\label{supp:info}
Supplementary information is organized to support reproducibility and interpretation. \hyperref[supp:appendix-a]{Appendix A} records the corpus ledger, annotation ontology, corpus-scale annotation workflow, validation evidence and model specifications. \hyperref[supp:appendix-b]{Appendix B} contains the supporting tables and model outputs referenced from the main text and Appendix A. \hyperref[supp:appendix-c]{Appendix C} contains the supporting figures.

\makeatletter
\setlength{\@fptop}{0pt}
\setlength{\@fpsep}{8pt plus 2pt}
\setlength{\@fpbot}{0pt plus 1fil}
\makeatother


\providecommand{\appcomment}[1]{\par\noindent\textcolor{orange!85!black}{\textbf{Comment addressed:} #1}\par}
\providecommand{\apporig}[1]{\par\noindent\textcolor{red}{\textbf{Original:} #1}\par}
\providecommand{\apprev}[1]{\par\noindent\textcolor{blue}{\textbf{Revised:} #1}\par}

\section*{Appendix A\quad Reproducibility protocol and construct ledger}
\phantomsection\label{supp:appendix-a}
\setcounter{table}{0}
\renewcommand{\thetable}{A\arabic{table}}

\subsection*{A.1\quad Corpus provenance and filtering ledger}
\phantomsection\label{supp:a1}

\revise{Supplementary Table~\ref{tab:corpus_filtering_pipeline} reports the corpus-specific filtering counts from public source records to the final analytic samples.}

\input{tables/table_corpus_filtering_pipeline}

\clearpage
  
\revise{Supplementary Table~\ref{tab:corpus_provenance_artifacts} records the public source links, source counting units and retained metadata fields used during corpus conversion.}

\input{tables/table_corpus_provenance_artifacts}

\revise{Supplementary Table~\ref{tab:filter_classifier_validation} maps each non-native filter or analytic label to its assignment procedure, validation evidence and role in the analyses.}

\input{tables/table_filter_classifier_validation}

\clearpage

\subsection*{A.2\quad Annotation ontology and operational examples}
\phantomsection\label{supp:a2}

\paragraph{Codebook and examples.}
The Methods summarize the role-specific constructs used in the analysis. This section provides the operational codebook, boundary rules and examples needed to reproduce the annotation decisions. Supplementary Table~\ref{tab:annotation_codebook_definitions} gives the formal label definitions; Supplementary Table~\ref{tab:label_examples} gives paraphrased examples without reproducing raw user text; and Supplementary Tables~\ref{tab:complete_coding_case} and \ref{tab:complete_writing_case} provide de-identified illustrative case excerpts. \revise{Support-intent and support-form profiles are visualized in Supplementary Figure~\ref{fig:supp_support_supply}.}

\input{tables/table_annotation_codebook_definitions}

\clearpage

\subsection*{A.3\quad Corpus-scale annotation implementation}
\phantomsection\label{supp:a3}

Implementation details for the annotation workflow are summarized in Supplementary Tables~\ref{tab:prompt_template_summary} and \ref{tab:annotation_implementation_controls}.

\input{tables/table_prompt_template_summary}

\input{tables/table_annotation_implementation_controls}

\clearpage

\subsection*{A.4\quad Human validation and corpus-scale label reliability}
\phantomsection\label{supp:a4}

\paragraph{Validation design.}
Human review served two measurement roles. First, human--human agreement assessed whether trained reviewers could apply the finalized codebook consistently. The coding codebook check combined three validation rounds covering 658 task turns, with 643 turns after deduplication; the writing check used 210 reviewed conversations. Supplementary Table~\ref{tab:label_validation} reports the per-label metrics for this codebook-operability layer. Second, human--LLM audits compared final LLM-assisted corpus-scale labels with human-confirmed labels after item-level review. These audits covered user framing, user engagement and assistant support across the six corpus-by-task settings and are reported in Supplementary Table~\ref{tab:production_label_validation}.

\paragraph{Agreement metrics.}
Agreement is reported with per-label F1, Matthews correlation coefficient (MCC) and Gwet's AC1 because the labels are sparse, prevalence-imbalanced and sometimes multi-label~\cite{matthews1975comparison,gwet2008computing}. \revise{For interpretation, we treated F1 values around 0.70 or higher as acceptable for a sparse boundary label, values around 0.80 or higher as strong, and values around 0.90 or higher as very strong. MCC and Gwet's AC1 were used as complementary prevalence-aware checks, with values around 0.60 indicating substantial agreement and values around 0.80 or higher indicating strong agreement.} In the human--human codebook checks, the focal labels used in the main analyses showed acceptable or better agreement: Constructive user engagement reached F1=0.85 in coding and F1=0.89 in writing, and scaffolded support reached F1=0.87 in coding and F1=0.97 in writing (Supplementary Table~\ref{tab:label_validation}). Complete per-label F1, MCC, Gwet AC1 and support values are reported in Supplementary Table~\ref{tab:label_validation}.

\paragraph{Corpus-scale label audits.}
The consolidated human--LLM audits were stratified across the six corpus-by-task settings. The user-framing audit reviewed 450 first user turns, with 75 cases from each setting. Intentional-framing agreement reached F1=0.857, MCC=0.677 and Gwet AC1=0.671. The user-engagement audit reviewed 600 user turns, with 100 cases per setting; the constructive-versus-non-constructive distinction reached F1=0.922, MCC=0.840 and Gwet AC1=0.837. The assistant-side audit reviewed 180 assistant turns, with 30 cases per setting; scaffolded-support agreement reached F1=0.912, MCC=0.590 and Gwet AC1=0.791, and support-form F1 values ranged from 0.691 to 0.938 across M1--M6. Supplementary Table~\ref{tab:production_label_validation} reports the corresponding precision, recall, accuracy and positive-count fields.

\paragraph{Post-labelling WildChat review sample.}
After the WildChat corpus-scale labelling pass, an additional label-composition check reviewed 100 labelled turns from WildChat coding and 100 labelled turns from WildChat writing, split into 50 user turns and 50 assistant turns per task. Supplementary Table~\ref{tab:wildchat_post_labelling_review} reports the observed corpus-scale label composition of these samples.

\clearpage

\subsection*{A.5\quad Statistical estimands and model specifications}
\phantomsection\label{supp:a5}

\paragraph{Notation.}
This subsection gives the statistical notation and model specifications used for the main contrast and regression analyses. Let $i$ index conversations, $s$ task settings, $j$ adjacent assistant--user turn pairs and $g$ user-framing strata. $C_i$ is the count of Constructive user turns, $Y_i$ indicates whether conversation $i$ contains at least one Constructive user turn, $S_i$ indicates at least one scaffolded assistant turn in the conversation and $X_i$ denotes the setting-specific conversation-level covariates listed in Supplementary Table~\ref{tab:setting_model_covariates}. GLM specifications follow standard Poisson and logistic forms~\cite{mccullagh1989generalized,agresti2013categorical}.

\paragraph{Conversation-level models.}
The adjusted count model in Section~2.3 is fitted separately within each setting:
\begin{align*}
C_i \mid S_i, X_i &\sim \operatorname{Poisson}(\mu_i),\\
\log(\mu_i) &= \alpha_s + \beta_s S_i + \gamma_s^\top X_i .
\end{align*}
The reported Poisson count ratio is $\exp(\beta_s)$. The companion binary model is
\begin{align*}
\operatorname{logit}\{\Pr(Y_i=1 \mid S_i, X_i)\}
  = \alpha_s + \beta_s S_i + \gamma_s^\top X_i ,
\end{align*}
with $\exp(\beta_s)$ reported as the odds ratio. The user-framing stratified count model uses
\begin{align*}
\log(\mu_i) = \alpha_{s,g} + \beta_{s,g} S_i + \gamma_{s,g}^{\top} X_i^{(-g)},
\end{align*}
where $X_i^{(-g)}$ omits user framing because the model is fitted within stratum $g$.
These models support the Section~2.3 adjusted scaffolded-support results in Fig.~\ref{fig:s2_conversation_association}b,c and Table~\ref{tab:support_engagement_summary}; setting-specific covariates are listed in Supplementary Table~\ref{tab:setting_model_covariates}.

\paragraph{Rate and context checks.}
The Section~2.2 constructive-rate sensitivity is a grouped-binomial logistic model:
\begin{align*}
C_i &\sim \operatorname{Binomial}(T_i,\pi_i),\\
\operatorname{logit}(\pi_i) &=
\alpha + \delta_1 F_i + \delta_2 Q_i + \delta_3 L_i + \delta_4 D_i ,
\end{align*}
where $T_i$ is the number of user turns, $F_i$ user framing, $Q_i$ task ecology, $L_i$ conversation-length bucket and $D_i$ dataset fixed effects. The Section~2.3 offset-rate sensitivity uses the same Poisson mean model as above with
\begin{align*}
\log(\mu_i) = \log(T_i) + \alpha_s + \beta_s S_i + \gamma_s^\top X_i .
\end{align*}
Supplementary Tables~\ref{tab:fig3_offset_rate_sensitivity} and \ref{tab:constructive_rate_context_logit} report the estimates.
The grouped-binomial model is the Section~2.2 sensitivity for the conversation-length pattern in Fig.~\ref{fig:engagement_ecology}c, whereas the offset-rate Poisson model is the Section~2.3 sensitivity for the scaffolded-support count models in Fig.~\ref{fig:s2_conversation_association}b.
\todayrevise{Supplementary Table~\ref{tab:section21_model_source_breakdown} reports a metadata breakdown for the Section~2.1 descriptive prevalence measures. Supplementary Tables~\ref{tab:section22_model_source_sensitivity} and \ref{tab:section23_model_source_sensitivity} report parallel metadata sensitivity checks in which dataset fixed effects are replaced by available assistant-model or service-source indicators. Supplementary Table~\ref{tab:section24_support_form_model_source_sensitivity} reports the corresponding Section~2.4 support-form sensitivity, restricted to scaffolded conversations and entering the six non-exclusive support-form labels jointly.}

\paragraph{Adjacent-turn models.}
Let $Z_{ij}$ indicate whether the next user turn after assistant turn $j$ is Constructive, $U_{ij}$ encode the immediately preceding user state, $A_{ij}$ encode scaffolded support at assistant turn $j$, $M_{ij}$ encode support-form labels M1--M6 and $R_{ij}$ denote task, framing, turn-position and fixed-effect controls. The integrated adjacent-turn model is
\begin{align*}
\operatorname{logit}\{\Pr(Z_{ij}=1)\}
  = \alpha + \theta^\top U_{ij} + \beta A_{ij}
  + \phi^\top M_{ij} + \eta^\top (U_{ij}\times M_{ij}) + \rho^\top R_{ij}.
\end{align*}
The broad scaffolded-support model omits $M_{ij}$ and $U_{ij}\times M_{ij}$; \todayrevise{metadata sensitivity checks replace dataset fixed effects in $R_{ij}$ with available assistant-model or service-source indicators}.
These models support the Section~2.5 adjacent-turn interpretation in Fig.~\ref{fig:temporal_coupling}d and Supplementary Tables~\ref{tab:integrated_regression}--\ref{tab:prior_state_support_form_interactions}; descriptive adjacent-turn lifts in Fig.~\ref{fig:temporal_coupling}a,b are summarized with bootstrap uncertainty in Supplementary Table~\ref{tab:key_contrast_significance}.

\paragraph{Uncertainty and multiplicity.}
Percentage-point contrasts use 1,000 conversation-level bootstrap resamples. Adjacent-turn contrasts use 1,000 conversation-cluster bootstrap resamples. \revise{Primary adjusted conversation-level GLMs use model-based standard errors, with HC1 or quasi-Poisson sensitivities reported where noted;} adjacent-turn regressions use conversation-cluster robust standard errors. Support-form bracket tests use Benjamini--Hochberg false-discovery-rate adjustment within each six-form comparison family~\cite{benjamini1995controlling}.

\paragraph{Metadata robustness checks.}
\latestrevise{Assistant-model metadata in WildChat and LMSYS Chat, and service-source metadata in ShareChat, provided an additional robustness layer for checking whether the reported patterns were stable across observable model and platform variation. These metadata enter the analyses in three ways. For Section~2.1, Supplementary Table~\ref{tab:section21_model_source_breakdown} reports descriptive ranges of engagement and scaffolding prevalence across metadata levels within each setting. For Sections~2.2--2.4, pooled sensitivity models refit the dataset-fixed-effect specifications after replacing dataset fixed effects with assistant-model or service-source fixed effects (Supplementary Tables~\ref{tab:section22_model_source_sensitivity}--\ref{tab:section24_support_form_model_source_sensitivity}). For Section~2.5, metadata-adjusted adjacent-turn regressions are reported both in pooled form and separately by corpus-by-task setting, because that model combines prior user state, assistant support and support form within local sequences (Supplementary Tables~\ref{tab:integrated_regression}--\ref{tab:prior_state_support_form_interactions}).}
\latestrevise{The tables should be read according to their estimand. Descriptive ranges show how prevalence measures vary across available assistant-model or service-source levels. In odds-ratio, count-ratio and rate-ratio tables, values above 1 indicate higher constructive engagement associated with the focal predictor, values below 1 indicate lower constructive engagement and confidence intervals excluding 1 indicate that the coefficient is statistically distinguishable from the null under that specification. The key robustness signal is preservation of direction and comparable magnitude when dataset fixed effects are replaced by metadata fixed effects. Pooled tables summarize corpus-wide stability; setting-level adjacent-turn tables show whether the local sequence pattern is shared across corpus-by-task settings.}

\section*{Appendix B\quad Source tables and model outputs}
\phantomsection\label{supp:appendix-b}
\setcounter{table}{0}
\renewcommand{\thetable}{B\arabic{table}}

\input{tables/table_label_examples}

\input{tables/table_complete_case_examples}

\input{tables/table_label_validation}

\clearpage

\input{tables/table_production_label_validation}

\clearpage

\input{tables/table_wildchat_post_labelling_review}

\input{tables/table_setting_model_covariates}

\clearpage

\input{tables/table_key_contrast_significance}

\clearpage

\input{tables/table_fig3_offset_rate_sensitivity}

\input{tables/table_section21_model_source_breakdown}

\clearpage

\input{tables/table_constructive_rate_context_logit}

\clearpage

\input{tables/table_section22_model_source_sensitivity}

\input{tables/table_section23_model_source_sensitivity}

\clearpage

\input{tables/table_section24_support_form_model_source_sensitivity}

\clearpage

\input{tables/table_integrated_regression}

\clearpage

\input{tables/table_setting_level_adjacent_models}

\clearpage

\input{tables/table_integrated_scaffolding_block_tests}

\input{tables/table_prior_state_support_form_interactions}

\clearpage

\section*{Appendix C\quad Supporting figures}
\phantomsection\label{supp:appendix-c}
\setcounter{figure}{0}
\renewcommand{\thefigure}{C\arabic{figure}}

\paragraph{Support-form figures.}
\revise{Supplementary Figure~\ref{fig:supp_support_supply} combines the support-intent and support-form summaries across WildChat, LMSYS Chat and ShareChat.} Operational examples for individual support-form labels are provided in Supplementary Table~\ref{tab:label_examples}.

\paragraph{Model-snapshot figures.}
Supplementary Figure~\ref{fig:supp_model_family} provides a compact WildChat-only model-family robustness layer. \revise{It groups all available WildChat model snapshots that meet the model-fixed-effect sample threshold used in the robustness checks into four coarse families.} It asks whether the WildChat portion of the findings is concentrated in a particular user-facing model series or whether the same broad patterns remain visible across model families. LMSYS Chat and ShareChat remain represented in the six-setting tables and in the \todayrevise{metadata-adjusted regressions}, but they do not provide directly comparable user-facing model-snapshot structure for this visual repetition. The model-family figure is descriptive because model labels in naturalistic corpora are observational rather than experimentally assigned.

\begin{figure*}[!htbp]
\centering
\includegraphics[width=0.98\textwidth]{figures/supplementary/SupplementaryFigure_SupportIntentFormProfile.pdf}
\caption{\textbf{Support-intent and support-form profiles within scaffolded assistant turns.} \textbf{a--c,} Profiles for WildChat, LMSYS Chat and ShareChat, respectively. In each panel, top bars report the percentage of scaffolded assistant turns containing each support-form label (M1--M6), the central heatmap reports the percentage of turns with each support-intent label (I1--I3) that also contain each support-form label and right bars report the percentage of scaffolded assistant turns containing each support-intent label. \todayrevise{These values describe the composition of scaffolded assistant support; larger values indicate more frequent use of that support intent or form, not a larger estimated effect on user engagement.} Intent and support-form labels are non-exclusive, so values need not sum to 100\%.}
\label{fig:supp_support_supply}
\end{figure*}

\begin{figure*}[p]
\centering
\includegraphics[width=\textwidth]{figures/supplementary/SupplementaryFigure_WildChatModelFamilyRobustness.pdf}
\caption{\textbf{WildChat model-family robustness preserves the main directional patterns.} \revise{All available WildChat model snapshots meeting the model-fixed-effect sample threshold are shown as pale points and grouped into coarse model families.} Diamonds show family-level summaries. \textbf{a,} Cognitive engagement prevalence. \textbf{b,} Adjusted scaffolded-support association, reported as Poisson count ratios. \textbf{c,} Adjacent-turn constructive lift after scaffolded versus reference assistant turns. \todayrevise{In panel b, values above 1 indicate higher adjusted constructive-turn counts in conversations with scaffolded support; in panel c, positive values indicate a higher probability that the next user turn is constructive after scaffolded support.} The figure is descriptive because WildChat model labels are observational and may reflect time, user mix, task mix and logging context.}
\label{fig:supp_model_family}
\end{figure*}
